version: '2.13'

services:

  db:
    image: postgres:13
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ./db-data:/data/db
    environment:
      POSTGRES_USER: sfsb
      POSTGRES_PASSWORD: gFNQkM
      POSTGRES_DB: sfsb
      PGDATA: "/data/db"